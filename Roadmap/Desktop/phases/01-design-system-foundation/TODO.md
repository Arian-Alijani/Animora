# TODO — Phase 01: Design System Foundation

Generated by Step 0 on 2026-07-31. Source: PHASE.md Scope + `docs/architecture/12-desktop-architecture.md`
(theming/RTL section), `docs/TECH_STACK.md` §4, `docs/architecture/03-solution-structure.md`,
`desktop/AGENTS.md`, `docs/architecture/20-extensibility-playbook.md` ("add a desktop screen" steps
4-5), plus the product owner's reference screens extracted into
[`../../_meta/design-reference.md`](../../_meta/design-reference.md).

- [x] 1. Pin the look-and-feel packages in `Directory.Packages.props` and reference them from `Animora.Desktop.UI` only: `Semi.Avalonia`, `Semi.Avalonia.DataGrid`, `Avalonia.Controls.DataGrid`, `Projektanker.Icons.Avalonia` + `.MaterialDesign`, `DialogHost.Avalonia`, `CommunityToolkit.Mvvm`, `Microsoft.Extensions.DependencyInjection.Abstractions` (TECH_STACK §4, AG-08, DIR-07/AT-08)
- [x] 2. Dark palette decision: no dark reference screen is coming; the derived §3 table is accepted as the base and extended (surface-track, rail, accent tint/strong, state) by design decision — recorded in design-reference.md §3's addendum and in `Theme/Tokens/Colors.Dark.axaml`'s header (AG-02)
- [x] 3. Record the two folders this phase adds to `Animora.Desktop.UI` (`Assets/Fonts/`, `Services/`) in `desktop/AGENTS.md`'s project map and `docs/architecture/03-solution-structure.md`'s desktop tree (AG-13)
- [x] 4. Embed `Vazirmatn` 400/500/600/700 as `AvaloniaResource` under `Animora.Desktop.UI/Assets/Fonts/` and expose one `FontFamily` token resource over them (DESK-ARCH-13)
- [x] 5. Add `Theme/Tokens/Palette.axaml` with the raw brand/neutral/on-dark/accent ramps (design-reference §1.1-1.4) as `Color` resources, no semantic names yet (DESK-ARCH-12)
- [x] 6. Add `Theme/Tokens/Colors.Light.axaml` mapping palette ramps onto semantic keys (surface, page, border, divider, text, rail, accent tint/base/strong, state) for `ThemeVariant.Light` (DESK-ARCH-12)
- [x] 7. Add `Theme/Tokens/Colors.Dark.axaml` with the same semantic key set for `ThemeVariant.Dark` (design-reference §3 — depends on item 2's answer)
- [x] 8. Add `Theme/Tokens/Brushes.axaml`: solid brushes over the semantic colors plus the hero gradient, sparkline/area fill gradient, chart series brushes and heatmap ramp (design-reference §1.5)
- [x] 9. Add `Theme/Tokens/Typography.axaml`: the `FontFamily` default plus the `HeroTitle`/`PageTitle`/`SectionTitle`/`MetricValue`/`Body`/`BodyStrong`/`Caption`/`Overline` size, weight and line-height tokens (design-reference §2, DESK-ARCH-12)
- [x] 10. Add `Theme/Tokens/Layout.axaml`: spacing scale, corner-radius set, control heights/paddings, icon/avatar/accent-bar sizes, rail width, top-bar height (design-reference §4)
- [x] 11. Add `Theme/Tokens/Elevation.axaml`: card, hero and popup shadow tokens (design-reference §5)
- [x] 12. Add `Theme/AnimoraTheme.axaml` as the single include point that merges `Semi.Avalonia` + every token dictionary + every style file, so a consumer references one URI and nothing else (DIR-07, DESK-ARCH-12)
- [x] 13. Add `Theme/Styles/Root.axaml` establishing `FlowDirection=RightToLeft` and the default font on the top-level surface, documented as the one place phase 02's shell applies (DT-06, DESK-ARCH-06)
- [x] 14. Add `Theme/Styles/Typography.axaml`: `TextBlock`/`SelectableTextBlock` style classes bound to the typography tokens, one class per scale step (DESK-ARCH-12)
- [x] 15. Add `Theme/Styles/Buttons.axaml`: primary, secondary, subtle, danger and icon-button classes in the three control heights, all token-driven (design-reference §6)
- [x] 16. Add `Theme/Styles/Inputs.axaml`: `TextBox` (incl. the top-bar search variant), `ComboBox`, `CheckBox`, `RadioButton` over Semi's base themes (design-reference §6)
- [x] 17. Add `Theme/Styles/Surfaces.axaml`: `Border` classes for card, hero, tinted-accent, rail and callout surfaces including their radius/shadow tokens (design-reference §4.2, §5)
- [x] 18. Add `Theme/Styles/Chips.axaml`: status-chip variants (success/warning/danger/info/violet) and the count badge (design-reference §6)
- [x] 19. Add `Theme/Styles/DataGrid.axaml` over `Semi.Avalonia.DataGrid` with row/header tokens and virtualization left on by default (DT-08, TECH_STACK §4)
- [x] 20. Wire `Projektanker.Icons.Avalonia` (Material Design set) through a UI-owned registration helper plus icon-size tokens, so no consumer registers the provider itself (DESK-ARCH-13)
- [x] 21. Add `Controls/StatCard.axaml` (+ code-behind): icon tile, delta badge, metric value, label, footer note — tokens only (design-reference §6)
- [x] 22. Add `Controls/SectionCard.axaml`: title, optional eyebrow/overline, trailing action slot, content slot (design-reference §6)
- [x] 23. Add `Controls/StatusChip.axaml` and `Controls/CountBadge.axaml` driven by an accent-variant property (design-reference §6)
- [x] 24. Add `Controls/InsightCallout.axaml` and `Controls/EmptyState.axaml` (design-reference §6)
- [x] 25. Add `Localization/JalaliDateFormatter` over `PersianCalendar` with an injected UTC time source, plus unit tests (CONV-04/05/06, DESK-ARCH-14, DT-07)
- [x] 26. Add `Localization/PersianNumberFormatter` (Persian-Indic digits, grouping, percent) plus unit tests (design-reference §2)
- [x] 27. Add `Localization/MoneyFormatter` for IRR/Toman over `decimal`, plus unit tests (CONV-07, INV-05)
- [x] 28. Add the `Converters/` `IValueConverter` wrappers over the three formatters, plus converter unit tests; no formatting logic duplicated in the converters (DESK-ARCH-14, CONV-05)
- [x] 29. Add `Mvvm/ViewModelBase` on `CommunityToolkit.Mvvm` with no cross-screen state on it (DESK-ARCH-01/03)
- [x] 30. Add `Services/IDialogService` + its `DialogHost.Avalonia` implementation, returning results to callers without any module type (TECH_STACK §4, DIR-07)
- [x] 31. Add `Services/IToastService` + its `WindowNotificationManager` implementation, non-blocking only (DESK-ARCH-07/11, TECH_STACK §4)
- [x] 32. Add `Services/ServiceCollectionExtensions.AddDesktopUi()` registering the dialog/toast services, formatters and icon provider for phase 02's composition root to call once (DESK-ARCH-05 spirit, DIR-07)
- [x] 33. Add the `Avalonia.Headless` smoke test in `Animora.Desktop.UiTests`: `AnimoraTheme` loads, `FlowDirection` resolves to `RightToLeft`, Vazirmatn is the effective default family, and a `StatCard`/`StatusChip` tree renders with resolved token brushes (PHASE.md criterion 2, playbook "add a desktop screen" step 5)
- [x] 34. Add a token-discipline test that fails on literal colors, font sizes, font families or corner radii in any `Animora.Desktop.UI` XAML outside `Theme/Tokens/` (PHASE.md criterion 5, DT-06)
- [x] 35. Add a throwaway token gallery window in `Animora.Desktop.App` showing every token group and control, marked `TODO(P1-01): delete after the phase-01 host check` (CM-06)
- [x] 36. User-run on Windows: the token gallery renders RTL with Vazirmatn, token colors and shadows as in the reference screens (`dotnet run --project desktop/src/Animora.Desktop.App`)
- [x] 37. Verify phase Completion Criteria (see PHASE.md), delete the item 35 gallery, and update PROGRESS.md to `complete`
