# TODO — Phase 00: Solution Bootstrap

Generated by Step 0 on 2026-07-31. Source: PHASE.md Scope + `docs/architecture/03-solution-structure.md`,
`desktop/AGENTS.md`, `docs/TECH_STACK.md` §2/§4 (+§15 CI shape, §16 test stack).

- [x] 1. Make a .NET 10 LTS SDK available in the dev environment and record the verified `dotnet --version`; add a bootstrap script under `tools/scripts/` only if the environment needs one (TECH_STACK §2, AG-13)
- [x] 2. Add `shared/dotnet/Animora.SharedKernel/Animora.SharedKernel.csproj` and `shared/dotnet/Animora.Contracts/Animora.Contracts.csproj` as leaf class libraries with no types yet — content belongs to phase 03 (DIR-02, AT-02, AG-06)
- [x] 3. Add `desktop/src/Animora.Desktop.UI/Animora.Desktop.UI.csproj` referencing only `SharedKernel` + `Contracts`, with the Avalonia 11 package reference pinned in `Directory.Packages.props`; theme packages stay for phase 01 (DIR-07, AT-08, TECH_STACK §2/§4)
- [x] 4. Add `desktop/src/Animora.Desktop.Data/Animora.Desktop.Data.csproj` referencing only `SharedKernel` + `Contracts`, with no EF Core/Dapper/SQLite packages yet (DT-05, AG-01)
- [x] 5. Add `desktop/src/Animora.Desktop.Infrastructure/Animora.Desktop.Infrastructure.csproj` referencing only `SharedKernel` + `Contracts`, with `Generated/` left empty (DT-12, AG-01)
- [x] 6. Add the nine `desktop/src/Animora.Desktop.Modules.<Name>/*.csproj` (Identity, Clients, Visits, Scheduling, Finance, Reporting, Notifications, Licensing, Files), each referencing `UI`, `Data`, `Infrastructure`, `SharedKernel`, `Contracts` and no sibling module (DIR-01, DIR-05, DT-01, AT-09)
- [x] 7. Add `desktop/src/Animora.Desktop.Sync/Animora.Desktop.Sync.csproj` as an empty seam referencing `Data` + `Infrastructure` only; module references wait for P2 (DT-12, AG-03)
- [x] 8. Add `desktop/src/Animora.Desktop.App/Animora.Desktop.App.csproj` as the Windows-targeted executable referencing every desktop project, with Avalonia desktop runtime + `Microsoft.Extensions.Hosting` pinned centrally (DIR-04 desktop analogue, TECH_STACK §4)
- [x] 9. Add `Startup/Program.cs` building a `Microsoft.Extensions.Hosting` generic host that owns the Avalonia lifetime — no module registration, no navigation service (TECH_STACK §4, AG-13)
- [x] 10. Add `App.axaml` + `App.axaml.cs` and one empty `Shell/` window shown at startup; no shell chrome, no `FlowDirection` root setting, no theme (phase 01/02 own those — DT-06, DESK-ARCH-06)
- [x] 11. Add `desktop/Animora.Desktop.sln` listing every desktop project plus the two `shared/dotnet/*` projects it references, so `dotnet build`/`dotnet test` from `desktop/` covers the whole graph
- [x] 12. Add `desktop/tests/Animora.Desktop.UnitTests/*.csproj` with xUnit + FluentAssertions + NSubstitute pinned centrally, zero tests (TECH_STACK §16)
- [x] 13. Add `desktop/tests/Animora.Desktop.UiTests/*.csproj` with xUnit + `Avalonia.Headless`, zero screen tests yet (TECH_STACK §16, desktop screen DoD)
- [x] 14. Add `desktop/tests/Animora.Desktop.ArchTests/*.csproj` with xUnit + `NetArchTest`, zero rules yet
- [x] 15. Implement the desktop assembly-boundary rules AT-08 and AT-09 in `ArchTests` (DIR-01, DIR-05, DIR-07)
- [x] 16. Implement the desktop persistence-boundary rules AT-03/AT-04/AT-05 in `ArchTests` (vacuous until phase 14) and mark AT-07 with `TODO(P1-03)` since `ISyncedEntity` does not exist yet (INV-20, DT-05, CM-06)
- [x] 17. Add a CI-shaped local script under `tools/scripts/` running restore → build → test for the desktop solution and confirm it exits green (TECH_STACK §15, AG-13)
- [x] 18. Ask the user: how is "the Avalonia app launches to an empty window" to be verified for sign-off, given the agent environment has no Windows host or display — Windows machine run by the user, or an `Avalonia.Headless` startup assertion? (AG-02, AG-19) — answered 2026-07-31: the user ran the app on their own Windows machine and the window came up; sign-off is that real run, no headless launch test added. Logged in [`../../_meta/host-verification-log.md`](../../_meta/host-verification-log.md)
- [x] 19. Verify phase Completion Criteria (see PHASE.md) and update PROGRESS.md to `complete` (launch criterion depends on item 18's answer)

## Notes

- Item 18's answer set a roadmap-wide convention, now written once in
  [`../../_meta/session-protocol.md`](../../_meta/session-protocol.md) "Host-dependent verification"
  and [`../../_meta/todo-format.md`](../../_meta/todo-format.md) "Host-dependent items": checks
  needing a real Windows host, display, printer, scanner, native notifications, installer, or
  real-hardware timing are scheduled as the second-to-last item of their phase and are run by the
  user on their machine, with the outcome appended to
  [`../../_meta/host-verification-log.md`](../../_meta/host-verification-log.md).
