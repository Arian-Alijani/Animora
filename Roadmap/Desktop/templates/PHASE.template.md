# Phase NN: <Name>

## Goal

One or two sentences: the single clear objective of this phase. No TODO detail here.

## Expected Outcome

The meaningful milestone this phase produces — what exists and is demonstrably true once the phase
is `complete`.

## Scope

Bullet list of what is in bounds for this phase. Explicitly note what is deferred to a later phase
if the boundary is likely to be ambiguous (e.g., "screens only, no local persistence — see phase
NN+X").

## Key References

The 2-4 docs (AG-07 budget) Step 0 must read for this phase, and why each is relevant. Do not list
more than 4; if more seem necessary, that is itself a signal recorded here for Step 0 to flag.

## Dependencies

Which phases must be `complete` first (per `PROGRESS.md` sequencing rules), and what this phase
hands to later phases.

## Completion Criteria

A short, checkable list. Step 0's final generated TODO item re-verifies these explicitly before the
phase is marked `complete` in `PROGRESS.md`.

---

## Step 0

Not run yet. When executed, Step 0 reads this file's Goal/Scope/Key References plus the cited
architecture docs and generates `TODO.md` in this same folder, per
[`_meta/todo-format.md`](../../_meta/todo-format.md) and the procedure in
[`_meta/session-protocol.md`](../../_meta/session-protocol.md). Do not generate TODO items here.
