# Animora Roadmap

Build order (see repo-root [`AGENTS.md`](../AGENTS.md) §1): **Desktop -> Backend -> Web**.

Only [`Desktop/`](Desktop/README.md) exists today. `Roadmap/Backend/` and `Roadmap/Web/` are created
the same way, once desktop is signed off — do not create them early (matches AG-04: phase order is
a hard constraint).
